import BlogPost from "../../../components/BlogPost";
import Code from "../../../components/Code";

export const meta = {
  title: "Array - copy/clone array",
  description: "Copy or clone an array without mutate",
};

export default ({ children }) => <BlogPost meta={meta}>{children}</BlogPost>;

Cloning array with **concat()**, **slice()** or **spread operator** without mutate the initial one

<Code>
  {`const numbers = [2, 4, 6, 8, 10];\n
// 1. with concat
const copy = [].concat(numbers);\n
// 2. with slice
const copy = numbers.slice();\n
// 2. with spread operator
const copy = [...numbers];\n
// ➔ copy = [2, 4, 6, 8, 10]`}
</Code>
