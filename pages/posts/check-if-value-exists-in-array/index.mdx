import BlogPost from "../../../components/BlogPost";
import Code from "../../../components/Code";

export const meta = {
  title: "Array - check if value exists",
  description: "Check if value exists in array",
};

export default ({ children }) => <BlogPost meta={meta}>{children}</BlogPost>;

You can check if a value exists with a few methods :

<Code>
  {`const sports = ["soccer", "hockey", "basketball", "baseball", "ski"];\n
//1. with indexOf
sports.indexOf("hockey") !== -1 // true\n
// 2. with includes
sports.includes("hockey"); // true\n
// 3. with some
const sports = [{id: 1, name: 'hockey'}, {id: 2, name: 'basketball'}];
sports.some(sport => sport.name === 'hockey'); // true\n
// . with find
const sport = sports.find(element => element === "hockey");
// ➔ sport = "hockey"`}
</Code>
