import BlogPost from "../../../components/BlogPost";
import Code from "../../../components/Code";

export const meta = {
  title: "How to reverse an array",
};

export default ({ children }) => <BlogPost meta={meta}>{children}</BlogPost>;

You can reverse an array by simply using the function [reverse](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/reverse).

<Code>
  {`const words = ["Hello", "World", "!"];
words.reverse();\n
// ➔ ["!", "World", "Hello"]`}
</Code>
