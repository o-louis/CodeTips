import BlogPost from "../../../components/BlogPost";
import Code from "../../../components/Code";

export const meta = {
  title: "Array - create a new array from the initial one",
  description: "Create a new array from the initial one",
};

export default ({ children }) => <BlogPost meta={meta}>{children}</BlogPost>;

The function **map()** return a new array from the initial one where the result is building by perfoming anything
you want on each element.

<Code>
  {`const numbers = [1, 2, 3, 4, 5, 6];
const even = numbers.map(number => number*2);\n
// even = [2, 4, 6, 8, 10, 12]`}
</Code>
